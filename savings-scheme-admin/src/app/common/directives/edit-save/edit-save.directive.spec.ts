import { EditSaveDirective } from './edit-save.directive';

describe('EditSaveDirective', () => {
  it('should create an instance', () => {
    const directive = new EditSaveDirective();
    expect(directive).toBeTruthy();
  });
});
